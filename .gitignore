# 반입용 소스 발췌 폴더 (Git 미관리)
LogViewer_source/

# 작업 Report 폴더 (Git 미관리)
report/

# Tomcat 설치 파일
apache-tomcat-*/
*.zip

# Python 컴파일 캐시 (실행 시 자동 생성됨)
__pycache__/
*.pyc

# Python 가상환경 (PC마다 재생성, 절대경로 포함되어 이식 불가)
.venv/

# editable 설치(pip install -e) 잔여물 (설치 시 자동 생성, PC마다 재생성)
*.egg-info/
